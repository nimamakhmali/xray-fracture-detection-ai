# تسک‌های کامل توسعه بخش AI — از صفر تا تحویل نهایی

---

## فاز 0 — زیرساخت و راه‌اندازی

---

### تسک 0.1 — تکمیل و بررسی محیط توسعه

محیط توسعه باید کاملاً آماده و تست‌شده باشد. در این تسک باید اطمینان حاصل شود که Python 3.10، CUDA، PyTorch و Ultralytics به درستی نصب شده‌اند و GPU به درستی شناسایی می‌شود. یک اسکریپت ساده اجرا می‌شود که وضعیت CUDA، نسخه PyTorch، تعداد GPU های موجود و مقدار حافظه GPU را گزارش دهد. در نهایت یک `yolo predict` نمونه روی یک تصویر ساده اجرا می‌شود تا از صحت نصب Ultralytics اطمینان حاصل شود. خروجی این تسک یک محیط کاملاً آماده است که هیچ خطایی در اجرای کدهای پایه ندارد.

---

### تسک 0.2 — تکمیل فایل‌های پیکربندی پایه

فایل‌های `.env.example`، `.gitignore` و `requirements.txt` باید کامل و دقیق باشند. در `.gitignore` باید پوشه‌های `data/`، `models/`، `mlruns/`، `logs/`، فایل‌های `.pt`، `.onnx`، فایل‌های کش Python و فایل‌های محیط مجازی به درستی تعریف شوند. در `requirements.txt` باید نسخه دقیق هر کتابخانه مشخص باشد و تفکیک بین `requirements.txt` (پکیج‌های اصلی) و `requirements-dev.txt` (پکیج‌های توسعه مثل pytest و black) حفظ شود. فایل `.env.example` باید تمام متغیرهای محیطی مورد نیاز پروژه را با مقادیر پیش‌فرض منطقی داشته باشد.

---

### تسک 0.3 — تکمیل فایل‌های پیکربندی مدل و دیتاست

فایل‌های `configs/model_config.yaml` و `configs/dataset.yaml` باید کامل شوند. در `model_config.yaml` باید پارامترهایی مثل نوع معماری، اندازه تصویر ورودی، تعداد کلاس‌ها، مسیر وزن‌های پیش‌آموزش‌دیده، پارامترهای آموزش مثل learning rate، batch size، تعداد epoch، و پارامترهای inference مثل confidence threshold و IOU threshold تعریف شوند. در `dataset.yaml` باید مسیر دقیق پوشه‌های train، val و test، نام و تعداد کلاس‌ها، و مشخصات کلی دیتاست تعریف شوند. این فایل‌ها باید config-driven باشند یعنی هر تغییری در رفتار سیستم از اینجا اعمال شود نه از داخل کد.

---

## فاز 1 — آماده‌سازی داده

---

### تسک 1.1 — بررسی و تحلیل دیتاست‌های دانلودشده

قبل از هر کاری باید هر دو دیتاست GRAZPEDWRI-DX و FracAtlas کاملاً بررسی و شناخته شوند. این بررسی شامل فهمیدن ساختار پوشه‌بندی هر دیتاست، فرمت annotation های موجود (آیا YOLO است، COCO است، Pascal VOC است یا چیز دیگری)، تعداد کل تصاویر، تعداد تصاویر دارای شکستگی در مقابل تصاویر سالم، توزیع resolution تصاویر، فرمت فایل‌ها (JPG، PNG، DICOM) و کیفیت برچسب‌گذاری‌ها می‌شود. خروجی این تسک یک گزارش مکتوب در notebook `01_data_exploration.ipynb` است که شامل آمار کامل هر دو دیتاست، نمونه تصاویر، توزیع کلاس‌ها و مشکلات احتمالی است.

---

### تسک 1.2 — تبدیل و یکسان‌سازی فرمت annotation ها

هر دو دیتاست باید به فرمت استاندارد YOLO تبدیل شوند. فرمت استاندارد YOLO برای هر شیء در یک خط است به صورت `class x_center y_center width height` که همه مقادیر نرمال‌شده بین 0 و 1 هستند. اسکریپت `scripts/prepare_dataset.py` باید این تبدیل را انجام دهد. نکات مهم در این تسک این است که اگر یک دیتاست چندین نوع آسیب دارد، باید تصمیم گرفته شود که آیا همه را به کلاس واحد `fracture` تبدیل کنیم یا نه (در فاز اول همه را یک کلاس در نظر می‌گیریم). همچنین باید بررسی شود که هیچ annotation خارج از محدوده تصویر نباشد و مختصات معتبر باشند.

---

### تسک 1.3 — ترکیب دیتاست‌ها و تقسیم‌بندی

دو دیتاست باید به شکل هوشمند ترکیب شوند. ابتدا تصاویر تکراری یا بسیار مشابه شناسایی و حذف شوند. سپس کل دیتاست ترکیب‌شده به نسبت 70 درصد train، 15 درصد validation و 15 درصد test تقسیم شود. این تقسیم‌بندی باید stratified باشد یعنی نسبت تصاویر دارای شکستگی به تصاویر سالم در هر سه بخش یکسان باشد. مهم است که هیچ تصویری بین train و test مشترک نباشد. پوشه‌بندی نهایی در `data/processed/` به شکل `train/images`، `train/labels`، `val/images`، `val/labels`، `test/images`، `test/labels` ایجاد شود.

---

### تسک 1.4 — پیاده‌سازی validator.py

این ماژول مسئول بررسی سلامت دیتاست است و باید قابلیت‌های زیر را داشته باشد: بررسی وجود فایل label برای هر تصویر، بررسی معتبر بودن مختصات در label ها، بررسی فرمت درست فایل‌های label، شناسایی تصاویر خراب یا ناقص، گزارش تعداد تصاویر بدون annotation، و گزارش آماری کلی از وضعیت دیتاست. این ماژول هم به صورت مستقل قابل اجرا باشد و هم قابل import در سایر ماژول‌ها. خروجی آن یک گزارش ساختاریافته باشد که نشان دهد دیتاست آماده آموزش است یا خیر.

---

### تسک 1.5 — پیاده‌سازی loader.py

این ماژول مسئول بارگذاری و آماده‌سازی داده برای آموزش است. باید بتواند تصاویر را از مسیر مشخص بخواند، label های متناظر را بارگذاری کند، و داده را در قالب مناسب برای YOLO آماده کند. همچنین باید آمار کلی دیتاست بارگذاری‌شده را گزارش دهد. این ماژول باید با `dataset.yaml` کار کند و مسیرها را از آن بخواند نه از hardcode.

---

## فاز 2 — آموزش مدل پایه (Baseline)

---

### تسک 2.1 — پیاده‌سازی trainer.py و اسکریپت train.py

ماژول `src/training/trainer.py` منطق اصلی آموزش را پیاده‌سازی می‌کند و اسکریپت `scripts/train.py` نقطه ورود اجرا است. در `trainer.py` باید منطق بارگذاری مدل، تنظیم پارامترهای آموزش از روی config، اجرای آموزش، ذخیره checkpoint های میانی، و ذخیره بهترین مدل پیاده‌سازی شود. در `scripts/train.py` باید بتوان پارامترها را از command line هم دریافت کرد تا بتوان بدون تغییر کد، آموزش‌های مختلف اجرا کرد. MLflow برای ثبت تمام پارامترها، متریک‌ها و artifact های آموزش باید اینجا integrate شود.

---

### تسک 2.2 — اجرای اولین آموزش Baseline

با `yolov8n.pt` به عنوان نقطه شروع، اولین آموزش روی دیتاست آماده‌شده اجرا می‌شود. این آموزش با تنظیمات پیش‌فرض و 50 epoch اجرا می‌شود. هدف از این تسک صرفاً گرفتن یک baseline برای مقایسه است نه رسیدن به دقت نهایی. در طول آموزش باید منحنی loss و mAP50 در MLflow قابل مشاهده باشد. اگر loss دیورژ شد یا آموزش به مشکل خورد باید debug شود. در پایان بهترین وزن ذخیره می‌شود و mAP50 روی validation set اندازه‌گیری می‌شود. هدف رسیدن به mAP50 حداقل 60 درصد است.

---

### تسک 2.3 — پیاده‌سازی evaluator.py و اسکریپت evaluate.py

ماژول `src/training/evaluator.py` مسئول ارزیابی کامل مدل است. این ماژول باید مدل را روی test set اجرا کند و متریک‌های کامل شامل mAP50، mAP50-95، Precision، Recall و F1-Score را محاسبه کند. همچنین باید Confusion Matrix، Precision-Recall Curve رسم کند و نتایج را در یک فایل گزارش ذخیره کند. اسکریپت `scripts/evaluate.py` نقطه ورود اجرا است و باید بتوان مسیر مدل را به آن پاس داد تا هر مدلی را ارزیابی کند. نتایج ارزیابی باید در MLflow هم ثبت شوند.

---

## فاز 3 — پیش‌پردازش و بهینه‌سازی

---

### تسک 3.1 — پیاده‌سازی preprocessor.py

این ماژول پیش‌پردازش تخصصی تصاویر رادیولوژی را پیاده‌سازی می‌کند. مهم‌ترین تکنیک در اینجا CLAHE یعنی Contrast Limited Adaptive Histogram Equalization است که کنتراست تصاویر X-ray را بهبود می‌بخشد. علاوه بر این، نرمال‌سازی مقادیر پیکسل، تبدیل تصاویر grayscale به فرمت مناسب برای مدل، و resize کردن با حفظ نسبت ابعاد باید پیاده‌سازی شوند. این ماژول باید هم روی تصاویر JPG/PNG و هم روی فایل‌های DICOM کار کند. پیاده‌سازی پردازش DICOM با کتابخانه pydicom در همین ماژول انجام می‌شود. خروجی این ماژول تصویر آماده‌شده در قالب numpy array است.

---

### تسک 3.2 — پیاده‌سازی augmentation.py

این ماژول با کتابخانه albumentations پایپلاین افزایش داده را برای تصاویر رادیولوژی پیاده‌سازی می‌کند. augmentation های مناسب برای X-ray شامل چرخش‌های جزئی (حداکثر 15 درجه)، تغییر밝기 و کنتراست، اضافه کردن noise گاوسی که شبیه‌ساز نویز واقعی رادیولوژی باشد، و horizontal flip است. باید توجه شود که augmentation های غیرواقعی مثل تغییر رنگ یا چرخش‌های بزرگ برای تصاویر پزشکی مناسب نیستند. این ماژول باید دو حالت داشته باشد: حالت آموزش با augmentation کامل و حالت inference بدون augmentation. پایپلاین باید قابل تنظیم از طریق config باشد.

---

### تسک 3.3 — بازآموزی بهینه‌شده و Hyperparameter Tuning

با استفاده از پیش‌پردازش و augmentation پیاده‌سازی‌شده، آموزش جدید اجرا می‌شود. در این تسک باید چندین آزمایش با تنظیمات مختلف اجرا شود. آزمایش اول با `yolov8n` و augmentation، آزمایش دوم با `yolov8s` در صورتی که yolov8n به دقت 85 درصد نرسید، آزمایش‌هایی با اندازه‌های تصویر مختلف 640، 800 و 1024 پیکسل. در هر آزمایش پارامترهای learning rate، batch size و weight decay تنظیم می‌شوند. همه آزمایش‌ها در MLflow ثبت می‌شوند تا بتوان بهترین ترکیب را پیدا کرد. Early Stopping باید فعال باشد تا از overfitting جلوگیری شود. هدف رسیدن به mAP50 حداقل 85 درصد است.

---

### تسک 3.4 — اجرای benchmark.py و ارزیابی نهایی مدل

اسکریپت `scripts/benchmark.py` باید سرعت inference مدل را روی سخت‌افزارهای مختلف اندازه‌گیری کند. این اندازه‌گیری باید شامل زمان inference روی GPU، زمان inference روی CPU، throughput یعنی تعداد تصویر در ثانیه، و مصرف حافظه RAM و VRAM باشد. همچنین `scripts/evaluate.py` باید روی test set با بهترین مدل ذخیره‌شده اجرا شود و گزارش نهایی عملکرد تولید کند. این گزارش باید شامل تمام متریک‌ها و مقایسه با اهداف تعریف‌شده در پروپوزال باشد.

---

## فاز 4 — موتور Inference

---

### تسک 4.1 — پیاده‌سازی inference.py

این ماژول هسته اصلی استنتاج است و باید کاملاً مستقل از training باشد. وظایف آن شامل بارگذاری مدل ذخیره‌شده، دریافت تصویر ورودی به فرمت‌های مختلف (مسیر فایل، numpy array، bytes)، اجرای پیش‌پردازش از طریق preprocessor، اجرای مدل و گرفتن خروجی خام YOLO، و پاس دادن خروجی به postprocessor می‌شود. این ماژول باید thread-safe باشد چون ممکن است چند درخواست همزمان برسد. همچنین باید مکانیزم گرم نگه داشتن مدل در حافظه را داشته باشد تا در هر درخواست مجدداً مدل load نشود.

---

### تسک 4.2 — پیاده‌سازی postprocessor.py

این ماژول خروجی خام مدل YOLO را به خروجی مفید و ساختاریافته تبدیل می‌کند. پردازش‌هایی که باید انجام شوند عبارتند از: اعمال confidence threshold برای حذف detectionهای ضعیف، اعمال NMS یعنی Non-Maximum Suppression برای حذف bounding box های تکراری، تبدیل مختصات نرمال‌شده به مختصات پیکسلی، ساخت ساختار JSON استاندارد خروجی، و رسم bounding box روی تصویر اصلی. فرمت JSON خروجی باید شامل لیست detectionها، هر detection با مختصات bounding box، مقدار confidence، و label کلاس باشد. همچنین باید یک فیلد وضعیت کلی داشته باشد که نشان دهد آیا شکستگی تشخیص داده شده یا نه.

---

### تسک 4.3 — اجرای export_model.py

اسکریپت `scripts/export_model.py` باید مدل ذخیره‌شده PyTorch را به فرمت ONNX تبدیل کند. فرمت ONNX مزایای مهمی دارد: سرعت inference بالاتر روی CPU، قابلیت اجرا بدون نیاز به PyTorch، و سازگاری با پلتفرم‌های مختلف. بعد از export باید تست شود که خروجی مدل ONNX با خروجی مدل PyTorch یکسان است. همچنین باید سرعت inference هر دو فرمت مقایسه شود. اگر ONNX سرعت بهتری داشت، به عنوان مدل production استفاده می‌شود.

---

## فاز 5 — Explainability

---

### تسک 5.1 — پیاده‌سازی cam.py

این ماژول مسئول تولید Heatmap توضیح‌پذیری برای نمایش به پزشک است. رویکرد پیاده‌سازی در این پروژه Gaussian Heatmap مبتنی بر Bounding Box است که برای YOLO مناسب‌تر و سریع‌تر از Grad-CAM کلاسیک است. در این روش، برای هر bounding box تشخیص داده‌شده یک توزیع گاوسی ایجاد می‌شود که مرکز آن مرکز bounding box و شعاع آن متناسب با ابعاد bounding box است. سپس این heatmap روی تصویر اصلی با یک colormap مناسب (مثلاً قرمز برای نواحی با احتمال بالا) overlay می‌شود. خروجی نهایی یک تصویر است که تصویر اصلی X-ray را با bounding box و heatmap روی آن نشان می‌دهد. تصویر خروجی هم در فایل ذخیره شود و هم به صورت base64 encoded string قابل بازگشت باشد.

---

### تسک 5.2 — تست بصری و اعتبارسنجی خروجی explainability

خروجی‌های تولیدشده توسط cam.py باید روی مجموعه‌ای از تصاویر test set بررسی بصری شوند. هدف اطمینان از این است که heatmap ها واقعاً روی ناحیه شکستگی متمرکز شده‌اند و نه نواحی نامرتبط. نتایج بصری باید در notebook `03_evaluation.ipynb` مستند شوند. همچنین باید اطمینان حاصل شود که در حالتی که هیچ شکستگی تشخیص داده نشده، heatmap نمایش داده نمی‌شود و تصویر اصلی بدون تغییر برگردانده می‌شود.

---

## فاز 6 — AI Service و اتصال به Backend

---

### تسک 6.1 — پیاده‌سازی model_manager.py

این ماژول مسئول مدیریت چرخه حیات مدل است. وظایف آن شامل بارگذاری مدل در حافظه هنگام راه‌اندازی سرویس، نگه داشتن مدل در حافظه برای استفاده مجدد، امکان جایگزینی مدل در زمان اجرا بدون نیاز به restart سرویس، بازگرداندن اطلاعات مدل فعلی مثل نام، نسخه، تاریخ آموزش و متریک‌های ارزیابی، و مدیریت خطاهای مربوط به بارگذاری مدل می‌شود. این ماژول باید singleton pattern را پیاده‌سازی کند تا در طول اجرای سرویس فقط یک instance از مدل در حافظه وجود داشته باشد.

---

### تسک 6.2 — پیاده‌سازی ai_service.py به عنوان FastAPI Application

این مهم‌ترین تسک فاز 6 است. `ai_service.py` یک FastAPI application است که به عنوان سرویس مستقل اجرا می‌شود و backend سامانه از طریق HTTP با آن ارتباط برقرار می‌کند. Endpointهایی که باید پیاده‌سازی شوند عبارتند از:

**GET /health** که وضعیت سرویس و مدل بارگذاری‌شده را برمی‌گرداند و backend برای health check از آن استفاده می‌کند.

**POST /predict** که تصویر را دریافت می‌کند، inference کامل شامل پیش‌پردازش، استنتاج، پس‌پردازش و تولید heatmap را اجرا می‌کند، و نتیجه را به فرمت JSON برمی‌گرداند. این endpoint باید فرمت‌های JPG، PNG و DICOM را پشتیبانی کند.

**GET /model/info** که اطلاعات مدل فعلی را برمی‌گرداند.

**POST /model/reload** که مسیر یک مدل جدید را دریافت کرده و مدل را در حافظه جایگزین می‌کند. این endpoint برای قابلیت retraining پروژه استفاده می‌شود.

در تمام endpointها باید validation ورودی، مدیریت خطا، و logging کامل پیاده‌سازی شود. همچنین پاک‌سازی حافظه بعد از هر پردازش باید انجام شود.

---

### تسک 6.3 — پیاده‌سازی logging و مدیریت خطا در سرویس

ماژول `src/utils/logger.py` باید یک سیستم logging یکپارچه پیاده‌سازی کند. هر درخواست به سرویس باید با اطلاعاتی مثل زمان دریافت، نوع تصویر، زمان پردازش، نتیجه تشخیص و هرگونه خطا لاگ شود. لاگ‌ها باید در فایل ذخیره شوند و در environment production باید سطح لاگ قابل تنظیم باشد. برای خطاها باید error handling مناسب پیاده‌سازی شود تا اگر مدل با یک تصویر خاص به مشکل خورد، سرویس کرش نکند و پیام خطای مناسب برگرداند.

---

### تسک 6.4 — اصلاح و تکمیل Dockerfile و docker-compose.yml

Dockerfile باید برای serving بهینه شود نه training. باید از base image مناسب برای GPU استفاده شود. تمام dependency ها نصب شوند. فایل‌های مدل production کپی یا mount شوند. پورت 8000 expose شود. و دستور CMD اجرای سرویس FastAPI باشد نه train.py. در `docker-compose.yml` باید تنظیمات GPU، health check، volume های مناسب برای models و logs، و restart policy تعریف شوند. باید تست شود که سرویس داخل container به درستی اجرا می‌شود و endpoint های آن پاسخ می‌دهند.

---

## فاز 7 — تست، مستندسازی و تحویل

---

### تسک 7.1 — نوشتن Unit Testها

برای هر ماژول در `src/` باید unit test های متناظر در `tests/unit/` نوشته شود. تست‌های `test_preprocessor.py` باید موارد مختلف پیش‌پردازش مثل تصاویر با ابعاد مختلف، تصاویر DICOM و تصاویر خراب را پوشش دهند. `test_augmentation.py` باید صحت اعمال augmentation ها را بررسی کند. `test_validator.py` باید تمام حالت‌های دیتاست معتبر و نامعتبر را تست کند. `test_inference.py` باید با یک mock model اجرا شود. `test_postprocessor.py` باید تبدیل خروجی خام به JSON را تست کند. `test_cam.py` باید صحت تولید heatmap را بررسی کند. `test_model_manager.py` باید بارگذاری و جایگزینی مدل را تست کند. هدف پوشش بیش از 70 درصد کد است.

---

### تسک 7.2 — نوشتن Integration Testها

تست‌های integration در `tests/integration/` باید جریان کامل سیستم را از ورودی تا خروجی تست کنند. `test_pipeline.py` باید کل pipeline را از دریافت تصویر تا تولید JSON نهایی و heatmap تست کند. `test_api_integration.py` باید تمام endpoint های سرویس را با درخواست‌های HTTP واقعی تست کند شامل تصاویر معتبر، تصاویر نامعتبر، حالت بدون شکستگی و حالت با شکستگی. `conftest.py` باید fixture های مشترک مثل تصاویر test، مدل‌های mock و تنظیمات محیط test را تعریف کند.

---

### تسک 7.3 — تکمیل مستندات فنی و README

`README.md` باید شامل توضیح کلی پروژه، نحوه نصب و راه‌اندازی، نحوه اجرای آموزش مدل، نحوه اجرای سرویس، توضیح endpoint های API، نحوه اجرای تست‌ها، و معماری کلی سیستم باشد. علاوه بر README، باید یک مستند معماری AI تهیه شود که توضیح دهد هر ماژول چه کاری می‌کند، چطور با هم ارتباط دارند، و تصمیمات فنی مهم مثل انتخاب YOLO و روش explainability چرا گرفته شده. گزارش نهایی عملکرد مدل شامل تمام متریک‌ها، نمودارها و مقایسه با اهداف پروپوزال باید تولید شود.

---

### تسک 7.4 — تست نهایی یکپارچه و تحویل

قبل از تحویل رسمی باید یک تست end-to-end کامل انجام شود. در این تست یک تصویر X-ray واقعی از طریق سرویس Docker اجرا‌شده ارسال می‌شود و کل pipeline از دریافت تصویر تا تولید JSON نهایی با bounding box و heatmap بررسی می‌شود. سپس یک جلسه با تیم backend برگزار می‌شود و نحوه اتصال به سرویس، فرمت input و output، نحوه reload مدل، و مدیریت خطاها توضیح داده می‌شود. در نهایت همه کدها، مدل نهایی، مستندات و گزارش‌ها تحویل داده می‌شوند.

---

## جدول خلاصه تسک‌ها

| فاز | تسک | خروجی قابل اندازه‌گیری |
|-----|-----|----------------------|
| 0 | 0.1 — محیط توسعه | GPU شناسایی شده، yolo predict اجرا می‌شود |
| 0 | 0.2 — فایل‌های پیکربندی پایه | requirements، gitignore و env کامل |
| 0 | 0.3 — فایل‌های config مدل و دیتاست | model_config.yaml و dataset.yaml پر شده |
| 1 | 1.1 — تحلیل دیتاست‌ها | notebook تحلیل کامل دو دیتاست |
| 1 | 1.2 — تبدیل annotation ها | همه label ها در فرمت YOLO |
| 1 | 1.3 — ترکیب و تقسیم‌بندی | پوشه processed با ساختار train/val/test |
| 1 | 1.4 — validator.py | گزارش سلامت دیتاست |
| 1 | 1.5 — loader.py | بارگذاری موفق دیتاست |
| 2 | 2.1 — trainer.py و train.py | آموزش قابل اجرا با MLflow |
| 2 | 2.2 — Baseline training | mAP50 حداقل 60 درصد |
| 2 | 2.3 — evaluator.py | گزارش کامل متریک‌ها |
| 3 | 3.1 — preprocessor.py | پشتیبانی از JPG، PNG، DICOM |
| 3 | 3.2 — augmentation.py | pipeline augmentation تخصصی X-ray |
| 3 | 3.3 — بازآموزی بهینه | mAP50 حداقل 85 درصد |
| 3 | 3.4 — benchmark | گزارش سرعت CPU و GPU |
| 4 | 4.1 — inference.py | inference موفق روی تصاویر test |
| 4 | 4.2 — postprocessor.py | خروجی JSON استاندارد |
| 4 | 4.3 — export_model.py | مدل ONNX تولیدشده |
| 5 | 5.1 — cam.py | heatmap روی تصاویر تولید می‌شود |
| 5 | 5.2 — تست بصری | heatmap روی ناحیه شکستگی متمرکز |
| 6 | 6.1 — model_manager.py | بارگذاری و جایگزینی مدل |
| 6 | 6.2 — ai_service.py | همه endpoint ها پاسخ می‌دهند |
| 6 | 6.3 — logging و error handling | لاگ کامل هر درخواست |
| 6 | 6.4 — Docker | سرویس داخل container اجرا می‌شود |
| 7 | 7.1 — Unit Tests | پوشش بیش از 70 درصد |
| 7 | 7.2 — Integration Tests | تمام سناریوها تست شده |
| 7 | 7.3 — مستندات | README و مستند معماری کامل |
| 7 | 7.4 — تحویل نهایی | تست end-to-end موفق |